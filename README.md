# homebrew-tap

Homebrew tap for [chainrecon](https://github.com/chainrecon/chainrecon).

```bash
brew install chainrecon/tap/chainrecon
```

This tap is updated automatically by goreleaser on each release.